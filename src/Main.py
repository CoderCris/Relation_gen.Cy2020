from random import randint
from sys import argv

script, polarity = argv

enterprises = open(enterprises)
relationships = open(relationships)



def main ()
    loadFiles()
    print define()


def define():

    return enterprise() + relationship(roll, polarity) + enterprise(roll)

def enterprise():

    while(enterprises.readLine() != null)
        #¡¡¡lo deje aqui por pereza, pero la idea es que esto cuente el numero delineas
        #escritas para poder hacer la tirada random, no es elegante, pero es funcional


def relationship():



def int roll ():

    return randint(0, faces-1)
